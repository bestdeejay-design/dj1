# 🔍 SCRIPT.JS АУДИТ - КРИТИЧЕСКИЙ ФУНКЦИОНАЛ

## Цель аудита
Проверить что ВСЕ ключевые функции работают корректно и надёжно.

---

## 📋 СПИСОК КРИТИЧЕСКОГО ФУНКЦИОНАЛА

### 1️⃣ ПРОКРУТКА (Scroll)

**Функции:**
- `scrollToCurrentTopTrack()` - скролл к текущему треку в топе
- `scrollToCurrentTagTrack()` - скролл к треку по тегу
- `scrollToCurrentAlbum()` - скролл к альбому

**Что проверить:**
- ✅ Скролл работает при переключении трека
- ✅ Скролл работает после обновления страницы
- ✅ Трек центрируется (block: 'center')
- ✅ Плавная прокрутка (behavior: 'smooth')
- ✅ Задержки достаточные для DOM update

**Проблемы:**
- ❌ [Записать если есть]

---

### 2️⃣ ПОДСВЕТКА (Highlight)

**Функции:**
- `updateTopTrackHighlight()` - подсветка в топе
- `updateTagTrackHighlight()` - подсветка по тегу
- `highlightPlaylistItem()` - подсветка в плейлисте

**Что проверить:**
- ✅ Класс `.playing` добавляется правильному элементу
- ✅ Используется data-track-id а не индекс
- ✅ Подсветка работает после обновления страницы
- ✅ Предыдущий трек снимает подсветку

**Проблемы:**
- ❌ [Записать если есть]

---

### 3️⃣ СОХРАНЕНИЕ СОСТОЯНИЯ (State Persistence)

**Функции:**
- `savePlayerState()` - сохранение в localStorage
- `restorePlayerState()` - восстановление из localStorage

**Что сохраняется:**
- ✅ albumId - ID альбома/вида
- ✅ albumTitle - название
- ✅ albumCover - обложка
- ✅ trackIndex - индекс трека
- ✅ trackId - ID трека (критично!)
- ✅ currentTime - позиция воспроизведения
- ✅ isPlaying - состояние play/pause
- ✅ timestamp - время сохранения

**Что проверить:**
- ✅ Все поля сохраняются
- ✅ trackId НЕ undefined
- ✅ Восстановление находит трек по ID
- ✅ currentTrackIndex устанавливается правильно
- ✅ Виртуальный альбом имеет id у треков

**Проблемы:**
- ❌ [Записать если есть]

---

### 4️⃣ ВОССТАНОВЛЕНИЕ ПОСЛЕ ОБНОВЛЕНИЯ СТРАНИЦЫ

**Сценарий:**
1. Включить трек
2. Дать поиграть 10-15 секунд
3. Обновить страницу (F5)
4. Проверить восстановление

**Ожидаемый результат:**
- ✅ Вид (view) восстановился (Albums/Top Tracks/Tags)
- ✅ Тема (theme) восстановилась
- ✅ Трек в плеере тот же самый
- ✅ Позиция трека та же (currentTime)
- ✅ Трек подсвечивается в списке
- ✅ Трек проскроллен к центру
- ✅ Можно нажать Play и продолжить

**Проблемы:**
- ❌ [Записать если есть]

---

### 5️⃣ ВИРТУАЛЬНЫЙ АЛЬБОМ (Virtual Album)

**Места создания:**
- `restoreTopTrackPlayer()` - для топа треков
- `restoreTagTrackPlayer()` - для треков по тегу
- `playTopTrack()` - при запуске из топа

**Структура трека:**
```javascript
{
    id: t.id,              // ✅ КРИТИЧНО: должен быть!
    name: t.name,
    file: t.file,
    cover: t.cover,
    duration: t.duration,
    sound: t.sound,
    lyrics: t.lyrics,
    model: t.model
}
```

**Что проверить:**
- ✅ Все поля включая `id` присутствуют
- ✅ `id` используется для поиска и сохранения
- ✅ `currentTrackIndex` устанавливается после создания

**Проблемы:**
- ❌ [Записать если есть]

---

### 6️⃣ ПЕРЕКЛЮЧЕНИЕ ТРЕКОВ (Track Switching)

**Функции:**
- `nextTrack()` - следующий трек
- `prevTrack()` - предыдущий трек
- `selectTrack()` - выбор трека

**Что проверить:**
- ✅ nextTrack() вызывает скролл и подсветку
- ✅ prevTrack() вызывает скролл и подсветку
- ✅ selectTrack() обновляет currentTrackIndex ДО подсветки
- ✅ Задержки достаточные (100ms)

**Проблемы:**
- ❌ [Записать если есть]

---

### 7️⃣ ОЧИСТКА СОСТОЯНИЯ (Clear State)

**Функция:**
- `clearAllStateAndReload()`

**Что очищается:**
- ✅ localStorage.removeItem('playerState')
- ✅ localStorage.removeItem('currentView')
- ✅ localStorage.removeItem('theme')
- ✅ audioPlayer.pause(), currentTime = 0, src = ''
- ✅ currentAlbum = null
- ✅ currentTrackIndex = -1
- ✅ DOM очищается

**Что проверить:**
- ✅ Кнопка ✕ видна и кликабельна
- ✅ Все данные очищаются
- ✅ Страница перезагружается через 300ms

**Проблемы:**
- ❌ [Записать если есть]

---

## 🐛 НАЙДЕННЫЕ ПРОБЛЕМЫ

| № | Проблема | Критичность | Статус |
|---|----------|-------------|--------|
| 1 | Track not found by ID: undefined | 🔴 Critical | ✅ Fixed |
| 2 | currentTrackIndex не установлен | 🔴 Critical | ✅ Fixed |
| 3 | Нет id в виртуальном альбоме | 🔴 Critical | ✅ Fixed |
| 4 | ... | ... | ... |

---

## ✅ ЧЕКЛИСТ ИСПРАВЛЕНИЙ

### Сделано:
- ✅ Добавлен `id: t.id` в restoreTopTrackPlayer()
- ✅ Добавлен `id: t.id` в restoreTagTrackPlayer()
- ✅ Добавлен `id: t.id` в playTopTrack()
- ✅ Установлен currentTrackIndex в restoreTopTrackPlayer()
- ✅ Установлен currentTrackIndex в restoreTagTrackPlayer()
- ✅ Увеличены задержки: 300ms → 500ms для скролла
- ✅ Добавлено логирование: console.log('✓ Top track highlight updated')

### Требует проверки:
- ⏳ Прокрутка работает после обновления
- ⏳ Подсветка работает после обновления
- ⏳ trackId сохраняется корректно
- ⏳ Все view восстанавливаются

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **ТЕСТИРОВАНИЕ** - проверить каждый пункт вручную
2. **ИСПРАВЛЕНИЕ** - исправить найденные проблемы
3. **СТАБИЛИЗАЦИЯ** - убедиться что ничего не ломается
4. **ДОКУМЕНТАЦИЯ** - обновить комментарии в коде
5. **ФАЗА 2** - продолжить работу над следующей фазой

---

## 📊 СТАТУС

- **Аудит начат**: [Дата]
- **Найдено проблем**: [Количество]
- **Исправлено**: [Количество]
- **Осталось**: [Количество]
- **Готово к Фазе 2**: ⏳

---

**Важно:** Не переходим к Фазе 2 пока все 🔴 проблемы не исправлены!
